# 집합의 특징
-중복 불가: 같은 원소를 2번 가져올수 없다.

-순서 없음: 원소의 운서가 없어서 인덱싱이 불가하다.

-용도: 중복된 데이터를 제거하거나 특정 데이터가 있는지 매우 빠르게 확인할때 사용한다.
# 집합의 종류
## 교집합: & intersection, 논리 AND (A 와 B에 있는 원소)
## 합집합: | union, 논리 OR (A 또는 B에 있는 원소)
## 차집합: - difference, 논리 NOT (A 중 B에 없는 원소)

# 기능
집합의 상태: s={1,2,3}
## 원소 하나 추가: add() -> s.add(4) => s={1,2,3,4} 이미 있는 값을 추가했을때는 변화가 없음.
## 원소 다량 추가: update() -> s.update(5,6,7) => s={1,2,3,4,5,6,7} 리스트, 또는 튜플로 여러개 추가
## 지정 원소 삭제: remove() -> s.remove(1) => s={2,3,4,5,6,7} 지정 원소가 없다면 Keyerror 발생
## 지정 원소 삭제(error X): discard() -> s.discard(10) => s={2,3,4,5,6,7} 지정 원소가 없더라도 Keyerror 발생 X
## 무작위 원소 삭제: pop() -> val = s.pop() => s={3,4,5,6,7} 무작위로 원소 하나 반환 (빈 집합일시 에러 발생)
## 전체 집합 삭제: clear() -> s.clear() => set() 모든 원소를 지워서 빈 집합으로 만듬

# 집합 관계 확인
a.isdisjoint(b) a와b에 겹치는 원소가 하나도 없으면 True
a.issubset(b) a가 b의 부분 집합이면 True
a.issuperset(b) a가 b를 포함하는 집합이면 True
x in s x가 s에 있는지 확인

문제
